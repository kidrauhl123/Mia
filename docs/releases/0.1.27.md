# Mia 0.1.27

- 修复云端 Agent 流式输出相邻文本块被拆成多个气泡的问题，回复会按同一次文本流合并展示。
- 增加本地 Agent Doctor：Hermes 会检查 CLI 与 API 依赖，Claude Code、Codex、OpenClaw 会做启动自检；失败时设置页和首次启动引导会显示可修复状态与修复入口。
- 修复移动端未读状态、前后台缓存同步和 Android SQLite runtime 隔离。
- 优化创建智能体弹窗的运行目标选择、桌面聊天消息动效和 MCP 文本样式。
- 部署 Cloud/Web 到当前桌面与移动端代码对应的 release manifest，确保生产 `/api/health` 可追踪到本次发布提交。
